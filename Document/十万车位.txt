十万车位怎么做

1、图片采用单独文件服务器(HTTP/Apache FTP/Serv-u)存储，利用文件系统的特性来查找图片；
对应的进出非图片信息存放在单独的数据库服务器上(使用表分区)，对应记录同时存储URL的根路径
/Images/Partition/a.jpg 多态服务器的考虑

2、或者采用独立的数据库服务器单独存储进出图片，非图片信息与图片的记录ID主键一起保存
多服务器的考虑


总数据库 本地数据库 内存数据库(rc = sqlite3_open(":memory:", &db);) 数据同步 初始化


业务线程 数据库线程 串口线程 视频线程 车牌线程 主线程 同一类可能包含多个线程　线程间的消息发送(信号返回结果)
理清线程间的交互关系



JSON /SP返回结果

JSON通用头 必有包含可选头的类型
JSON可选头
JSON体

XML/SP输入参数

XML通用头 必有包含可选头的类型
XML可选头
XML体

函数

解析通用头解析函数，根据通用头的可选头标志调用可选头解析函数，根据可选信息调用体解析函数

SP( in XML, out JSON );
SP( in XML ); 返回结果集，单或多结果集
